{
  "name": "${project.name}",
  "version": "${project.version}",
  "description": "${project.description}",
  "platform": "complete",
  "dependencies": [
    {
      "name": "app-consortia",
      "version": "^VERSION",
      "preRelease": "false"
    }
  ],
  "uiModules": [
    {
      "name": "folio_consortia-settings",
      "version": "^VERSION",
      "preRelease": "false"
    }
  ]
}
